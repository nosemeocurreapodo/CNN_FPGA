#pragma once

#ifdef IN_SIZE
const int in_size = IN_SIZE;
#else
const int in_size = IN_SIZE;
#endif

#ifdef OUT_SIZE
const int out_size = OUT_SIZE;
#else
const int out_size = OUT_SIZE;
#endif

#ifdef USE_RELU
const bool use_relu = USE_RELU;
#else
const bool use_relu = USE_RELU;
#endif
